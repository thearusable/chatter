$('#rooms-list').append('<%= j render @rooms %>');
<% if @rooms.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @rooms %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
