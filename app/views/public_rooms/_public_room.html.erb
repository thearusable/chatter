<div class="ui card">
  <div class="content">
    <div class="header"><%= link_to public_room.title, public_room_path(public_room) %></div>
    <div class="meta">
      <span><%= public_room.users.count %> users.</span>
    </div>
      <p><%= public_room.description %></p>
  </div>
</div>
